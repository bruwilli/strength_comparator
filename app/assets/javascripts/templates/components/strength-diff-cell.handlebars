{{#if includeArrow}}
  {{#if above}}
    <i class="fa fa-arrow-up"></i>
  {{else}}
    {{#if below}}
      <i class="fa fa-arrow-down"></i>
    {{/if}}
  {{/if}}
  {{absStrengthDiff}}
{{else}}
  {{strengthDiff}}
{{/if}}